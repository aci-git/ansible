---
- hosts: all
  gather_facts: no

  tasks:
    - name: Check for pending reboot
      win_reboot:
        reboot_required_var: pending_reboot_required

    - name: Output reboot status  
      debug:
        msg: "Reboot pending: {{ pending_reboot_required }}"
